# 验证记录

验证日期：2026-07-23

## 五层反证

1. **假设评分会漂移或无法解释**  
   相同资料与职位重复计算结果一致；分数拆为技能、岗位、经验、地点、排除词，硬阻断最多 49 分。

2. **假设工具可能编造经历或投错材料**  
   生成内容只读取候选人事实字段；每个材料输出 SHA-256 与 `source-map.json`，原资料不被覆盖。

3. **假设同一职位或批准会被重复使用**  
   规范化职位 URL 后以唯一指纹去重；提交批准绑定表单值快照、十分钟到期且只能消费一次。

4. **假设网页变化、验证码或失败会触发误提交**  
   验证码、未知必填、非唯一提交按钮、页面或字段变化均阻止点击；点击后先标记 `UNKNOWN`，不自动重试。

5. **假设界面能加载就代表可用**  
   使用合成身份完成资料保存、职位添加、96 分匹配、七份材料生成与下载；桌面和 375px 手机宽度无横向溢出，浏览器控制台无错误。

## 自动检查

- Python、前端 JavaScript、扩展 JavaScript 语法检查通过
- 25 个单元与契约测试通过
- HTTP 健康检查、资料保存、职位创建、材料生成、DOCX 下载与状态读取通过
- `start.cmd` 使用 Windows CRLF 换行，真实批处理启动与 `/api/health` 检查通过
- 扩展权限限制为主动标签页、脚本注入、本地存储和本机服务地址
- 动态界面不使用 `innerHTML` 或 `eval`
